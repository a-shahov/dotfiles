- clean: ['~']

- defaults:
    link:
        relink: true
        canonicalize: true

- link:
    ~/.config/kitty: kitty
    ~/.config/qtile: qtile

- shell:
    - [git submodule update --init --recursive, Installig submodules]
