- clean: ['~']

- defaults:
    link:
        relink: true
        canonicalize: true

- link:
    ~/.config/kitty: kitty
    ~/.config/qtile: qtile

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [./install_fonts.sh, Installing fonts]
